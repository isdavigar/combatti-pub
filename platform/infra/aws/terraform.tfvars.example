# Copia este archivo a terraform.tfvars y ajusta los valores.

region               = "us-east-1"
instance_type        = "t3.large"
cors_allowed_origins = "https://combatti-pub.pages.dev"

# --- Acceso SSH (opcional) ---
# ssh_key_name     = "mi-keypair"
# ssh_allowed_cidr = "203.0.113.10/32"   # tu IP/32

# --- Secretos (opcional; si se omiten, Terraform genera valores fuertes) ---
# jwt_secret          = ""
# seed_admin_password = ""
